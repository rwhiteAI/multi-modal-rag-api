# multi-modal-rag-api
A multimodal RAG API built with Flask, ChromaDB, and Ollama. Supports audio (Whisper), image (EasyOCR), and video (Moondream) ingestion with semantic search and local LLM querying.
